import subprocess 

process = subprocess.Popen(["python3 Start\\ Server.py"], shell = True)
process = subprocess.Popen(["python3 Index.py"], shell = True)
process = subprocess.Popen(["python3 Index.py"], shell = True)